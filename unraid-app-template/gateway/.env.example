# MCP Gateway Environment Variables
#
# Copy to .env and fill in values:
#   cp .env.example .env

# =============================================================================
# Tailscale
# =============================================================================

# Your Tailnet name (find at https://login.tailscale.com/admin/machines)
# This appears in your Tailscale machine URLs, e.g., "tower.tailnet-name.ts.net"
TAILNET_NAME=your-tailnet-name

# Tailscale auth key with Funnel capability
# Generate at: https://login.tailscale.com/admin/settings/keys
# - Check "Reusable"
# - Check "Ephemeral" (optional, for cleaner machine list)
# - Ensure your ACL allows Funnel for this key's tags
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Authelia
# =============================================================================

# Session secret (generate with: openssl rand -hex 32)
AUTHELIA_SESSION_SECRET=replace_with_random_hex_string

# OIDC client secret (plaintext - this is used by agentgateway to validate)
# The HASHED version goes in authelia/configuration.yml
OIDC_CLIENT_SECRET=your-client-secret-here

# =============================================================================
# Optional: Home Assistant Integration
# =============================================================================

# Home Assistant URL (if using Home Assistant MCP)
# HASS_URL=http://homeassistant:8123

# Home Assistant long-lived access token
# Generate in HA: Profile > Long-Lived Access Tokens
# HASS_TOKEN=your-long-lived-access-token

# =============================================================================
# Optional: Other MCP Backend Secrets
# =============================================================================

# Add environment variables for your MCP backends here
# Example:
# MY_API_KEY=secret-key
# DATABASE_URL=postgresql://user:pass@host/db
